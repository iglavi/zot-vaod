# רשימת משימות מאושרת — תיקוני דוח הבדיקה (giluy-naot.org.il)

מקור: דוח בדיקה חיצוני מ-2 באוגוסט 2026 על האתר החי.
סטטוס אישור: **כל הרשימה אושרה למעט סעיף 8 בדוח (הזדמנויות/Nice-to-have) — לא לבצע כרגע.**
סדר ביצוע מאושר: **קבוצה A ← B ← C ← D ← E.**

> הערה חשובה: חלק מהליקויים כבר תוקנו ב-`site-next/` אחרי שהדוח נכתב.
> פריטים כאלה מסומנים **[נראה שכבר טופל — לאמת]**. לגביהם: לוודא מול הדוח
> ולסגור, לא לבנות מחדש. כל השאר פתוח.

---

## קבוצה A — חוסמים (Blockers) — קודם כול

- [ ] **C-01 — חיבור מסמך↔רשומה שבור.** קישורי הורדת ה-Word/PDF ו/או שליפת גוף
  המסמך לא תמיד מוצאים את הקובץ הנכון (join בין הרשומה במאגר לקובץ ב-R2/דיסק).
  לוודא שכל רשומה מצביעה על ה-hash/מפתח הנכון, ושהורדה ופתיחת מסמך עובדות לכל תוצאה.
- [ ] **C-02 — `/api/search/options` החזיר 404.** **[נראה שכבר טופל — לאמת]**
  הנתיב קיים כעת ב-`site-next/api/search.py` ומזין את תפריטי סוג בית משפט/סוג הליך.
  לוודא שהוא מגיב 200 בפרודקשן ושהתפריטים נטענים.
- [ ] **C-03 — סטרימינג AI עמיד + retry.** להבטיח שתשובת ה-AI לא "נתקעת באמצע
  משפט" תחת עומס, ושיש טיפול בשגיאת רשת + ניסיון חוזר. (חלק מזה כבר נגע בקומיטים
  קודמים — לאמת שהתיקון יציב מקצה לקצה, כולל הודעת שגיאה ידידותית.)
- [ ] **H-08 — עמודים משפטיים חסרים.** להוסיף עמודי מדיניות פרטיות ותנאי שימוש
  (`/privacy`, `/terms`) + קישור אליהם מה-Footer. אין להם כרגע קיום ב-`site-next/app/`.
- [ ] **H-09 — ניקוי placeholders ושם בעמוד "אודות".** בעמוד `app/about/page.tsx`
  יש כרגע טקסטים זמניים: "מקום לצילום מסך של אתר נט-המשפט (טרם סופק)" ופעמיים
  "כאן יואל ישלים מידע." — להחליף בתוכן אמיתי (מודל ה-AI, מקור המסמכים) או להסיר
  את החלקים הריקים. לוודא שהשם "גילוי נאות" עקבי בכל האתר.

## קבוצה B — Quick wins

- [ ] **H-07 — שליחת טופס ב-Enter.** בחיפוש המובנה ובחיפוש ה-AI: לחיצת Enter
  בשדה טקסט תשלח את הטופס (לעטוף ב-`<form onSubmit>`), לא רק לחיצה על הכפתור.
- [ ] **H-01 — מיון ברירת מחדל.** בתוצאות החיפוש להגדיר מיון ברירת מחדל לפי
  תאריך יורד (החדש קודם) במקום "רלוונטיות" כברירת מחדל.
- [ ] **H-02 — תווית תקרת תוצאות.** כשמספר התוצאות מגיע לתקרה, להציג "מעל 5,000"
  (או המספר בפועל) במקום מספר קשיח/מטעה.
- [ ] **H-05 — מקורות ריקים כשאין התאמה.** בחיפוש AI, כשאין מסמכים רלוונטיים —
  לא להציג רשימת "מקורות" ריקה/מטעה; להבהיר שלא נמצאו מקורות תומכים.
- [ ] **M-06 — שינוי טקסט כפתור.** "שאלה חדשה" ← "שאלו".
- [ ] **M-01 — כתובות URL לשיתוף בחיפוש המובנה.** לשקף את פרמטרי החיפוש ב-query
  string כך שאפשר לשתף/לשמור קישור לתוצאות (deep link).
- [ ] **M-05 — הסרת ז'רגון פנימי + תיקון placeholder.** להסיר מונחים טכניים
  פנימיים מהטקסט הגלוי למשתמש, ולתקן טקסטי placeholder לא-ידידותיים בשדות.
- [ ] **קלט תקין — נרמול מספר תיק + ולידציה.** לנרמל פורמט מספר תיק, ולטפל
  בקלט קצה: טווח תאריכים הפוך (מ-אחרי-עד), `page=999999`, פרמטרים לא מוכרים —
  בלי קריסה ובלי תוצאות מטעות.

## קבוצה C — נגישות (Accessibility)

- [ ] **M-08 — מובייל.** גודל טקסט בסיס 16px, שטחי מגע ≥44px, תפריט המבורגר תקין.
  **[חלק תוקן — לאמת]** (סיידבר/ניווט מובייל כבר טופלו בקומיט קודם).
- [ ] **M-07 — ניגודיות גבולות שדות.** להעלות ניגודיות של מסגרות שדות קלט
  כדי שיהיו נראים בבירור.
- [ ] **M-10 — טבעת פוקוס (focus ring).** אינדיקציית פוקוס נראית לניווט מקלדת
  בכל הרכיבים האינטראקטיביים.
- [ ] **M-11 — סמנטיקה ו-SEO.** `h1` סמנטי אחד לעמוד, `alt` לתמונות, skip-link,
  `aria-live` לתוצאות/תשובות, `robots`, `sitemap.xml`, ותגי `og:`/Open Graph.
  **[חלק קיים — לאמת/להשלים]** ב-`app/layout.tsx` יש title/description ו-`lang`/`dir`,
  אך **חסרים** `openGraph`, `metadataBase`, `robots` ו-`sitemap`.

## קבוצה D — תוכן ונתונים

- [ ] **M-02 — פורמטי תאריך.** אחידות ותצוגת LTR לתאריכים. **[נראה שכבר טופל —
  לאמת]** (רכיב `DateLtr` + תאריכים LTR כבר נוספו).
- [ ] **M-12 — ציטוט מבקר המדינה: תיקון שגיאות + קישור.** בעמוד "אודות" הציטוט
  משובש: "יונגש בכלואי אפשרויות", "אפשרות אחזור וייצוא אחזור וניתוח" (כפילות),
  "שמעורכתיתה מעצמה", "הרשות השופטת בתי המשפט" (יתירות). לתקן לנוסח נכון ולהוסיף
  קישור לדוח מבקר המדינה 2022 המקורי.
- [ ] **H-10 — הצהרת כיסוי/שקיפות.** להוסיף הצהרה על היקף הכיסוי של המאגר
  (אילו בתי משפט/טווח תאריכים) ומגבלותיו, כדי שהמשתמש יבין מה כלול ומה לא.

## קבוצה E — משמעותי (מאמץ גדול)

- [ ] **H-04 — אחזור סמנטי (embeddings).** לשדרג את שליפת המסמכים ל-AI מ-BM25
  בלבד לאחזור סמנטי/וקטורי, לשיפור רלוונטיות התשובות.
- [ ] **H-06 — שאלות המשך + היסטוריה.** תמיכה בשיחת המשך (context) ושמירת
  היסטוריית שאלות בצ'אט ה-AI.
- [ ] **M-04 — ציטוטים מקושרים בטקסט.** ציטוטים בתוך תשובת ה-AI כקישורים
  ישירים למקור הרלוונטי.
- [ ] **H-03 — אינדוקס/ביצועים < 2 שניות.** לשפר זמן תגובת החיפוש לפחות מ-2 שניות.
- [ ] **M-09 — תקצירים/הדגשה/ייצוא.** snippets עם הדגשת מונח החיפוש, ואפשרות
  ייצוא תוצאות.
- [ ] **M-03 — מבנה "נטען מול נפסק".** בתשובות/תצוגת פסק דין, להפריד בין מה
  שנטען על ידי הצדדים לבין מה שנפסק בפועל.

---

## לא לביצוע כרגע (מוחרג)
- **סעיף 8 בדוח — הזדמנויות / Nice-to-have.** לא מאושר בשלב זה. לא לגעת.
